# Log

Append-only, chronological history of this wiki. Newest at the bottom.
Consistent prefixes keep it greppable: `## [YYYY-MM-DD] <ingest|query|lint|build> | <title>`.

---

## [2026-06-19] build | Knowledge base bootstrapped

- Established the LLM-Wiki structure per `CLAUDE.md` schema (raw/ → wiki/ → schema).
- Seeded the spine from `skill-set/2026/technology-skills.md` (2026 Solution Architect roadmap).
- Created Maps of Content: [[00-roadmap]], [[tier-1-edge]], [[tier-2-solid]], [[tier-3-watch]], [[meta-skills]].
- Generated 61 concept pages total (55 `stub`, ready to ingest); built `index.md` catalog.
- Wrote 6 research-backed `mature` exemplars for top P0 pages, each with live 2026 web research and citations (50 sources total): [[agentic-system-design]] (7), [[retrieval-augmented-generation]] (9), [[model-context-protocol]] (8), [[ai-specific-security]] (9), [[ai-generated-iac-reviewer]] (9), [[ai-agent-observability]] (8).

## [2026-06-19] lint | Bootstrap link-integrity pass

- Scanned all 75 markdown files for `[[wikilinks]]`: 79 unique targets across the vault.
- 0 broken links among real content. The 12 regex-flagged targets are all illustrative examples in `CLAUDE.md`/templates (`[[link]]`, `[[concept-one]]`, …) or a correctly escaped in-table wikilink (`[[policy-as-code\|…]]`).
- Promoted the 6 exemplars from `draft` → `mature` in `index.md` to match their on-page frontmatter.
- Coverage: every roadmap node (§1–§9 + meta) has a page; no orphans. Next: ingest sources to grow `stub` pages.

## [2026-06-19] build | Review improvements (Tier A + B)

- **Fixed citation defects** in mature pages: corrected a wrong arXiv id → [Tran & Kiela arXiv:2604.02460](https://arxiv.org/abs/2604.02460); added the [MAST / "Why Do Multi-Agent LLM Systems Fail?"](https://openreview.net/forum?id=fAjbYBmonr) citation for the 41–87% figure in [[agentic-system-design]]; softened an unsourced GraphRAG cost number in [[retrieval-augmented-generation]]; added the missing reciprocal RAG link.
- **Shipped executable lint** at `scripts/lint.ps1` (broken links / orphans / index gaps / stale mature) and rewrote `CLAUDE.md` §9.3 to call it. Run: `pwsh scripts/lint.ps1` → currently PASS (0 broken, 0 orphans).
- **Added [[dashboard]]** (Obsidian Bases `dashboard.base` + static backlog) ranking pages by priority × status; linked from README and index.

## [2026-06-19] ingest | Building Effective Agents (Anthropic)

- Source captured to [[2026-06-19-anthropic-building-effective-agents]] (raw/) via firecrawl.
- Promoted [[multi-agent-orchestration]] `stub → draft`, grounded in the five composable
  workflow patterns + orchestrator-workers and the workflows-vs-agents distinction.
- Updated `index.md` status; this is the worked example of the §9.1 ingest workflow.

## [2026-06-20] ingest | GraphRAG & Knowledge Graph Agent Memory series (Decoding AI, 5 articles)

Sources captured to `raw/graphrag-01` through `raw/graphrag-05` (articles 2026-01-20 through 2026-06-02, Anca Muscalagiu & Paul Iusztin).

**Series arc:** architecture design (articles 1–2) → Neo4j memory system internals (article 3) → ontology design (article 4) → graph hygiene pipeline (article 5).

- Created [[graphrag]] `draft` — covers: what GraphRAG is (data modeling problem, not retrieval algorithm), knowledge graph fundamentals, property graph vs RDF, two-phase generation (extraction → communities → summaries) + two-stage retrieval (RRF entry points → 2–3 hop traversal), bottom-up vs top-down, append-only vs mutable data models, DB selection (Postgres/MongoDB vs Neo4j), agentic GraphRAG via MCP `search_memory`/`write_memory` tools, production layering (graph=historical, MCP=real-time).
- Promoted [[agent-memory-architectures]] `stub → draft` — covers: 3 memory tiers (short-term/long-term/reasoning) joined by 3 typed edges, POLE+O ontology (5 fixed base types + subtypes), `:Fact` and `:Preference` primitives, 3-stage extraction cascade (spaCy → GLiNER → LLM), entity resolution vs deduplication (critical distinction), weighted dedup scores, `:SAME_AS` pending edge pattern for human review, dream pipeline (nightly re-dedup). Grounded in `neo4j-labs/agent-memory` reference implementation.
- Updated `index.md`: added `[[graphrag]] | draft`, bumped `[[agent-memory-architectures]]` to `draft`.

## [2026-06-20] ingest | Recursive Language Models (Decoding AI / Paul Iusztin)

- Source captured to `raw/recursive-language-models-decodingai.md` (article, 2026-04-07, grounded in arXiv:2512.24601).
- Created [[recursive-language-models]] `draft` — new concept page covering: REPL-based data navigation, root controller + worker sub-model architecture, `llm_query()`/`FINAL()` primitives, production guardrails (`maxIterations`, `maxDepth`, `maxStdoutLength`, sandboxing), four use cases (large file parsing, codebase comprehension, legal/financial analysis, deep research), and the RLM vs. RAG vs. CAG decision framework.
- Promoted [[context-engineering]] `stub → seed`: added the three-pattern comparison table (CAG / RAG / RLMs) and linked to the new page.
- Updated `index.md`: added `[[recursive-language-models]] | draft`, bumped `[[context-engineering]]` to `seed`.

## [2026-06-20] build | Wave 5 — 4 platform + integration + data stubs promoted to mature

- [[policy-as-code]] `stub → mature`: OPA/Rego as CNCF-graduated general-purpose policy engine; Gatekeeper v3.22 aligning with Kubernetes ValidatingAdmissionPolicy API (Feb 2026); Conftest for CI-time IaC policy evaluation (Terraform plan, Helm, Dockerfile); Kyverno (YAML-native, no new language, built-in mutation/generation/image verification); OPA vs. Kyverno decision table (language, mutation, complexity, Kubernetes alignment, best fit); HashiCorp Sentinel for HCP Terraform/Enterprise policy gates; enforcement mode spectrum (audit → advisory → mandatory) with rollout pattern; policy categories (security, operational, compliance, cost); dual enforcement pattern (CI Conftest + admission controller) for maximum coverage; AI-generated IaC as forcing function for PaC gates; pitfalls (jumping to enforce, no tests, overlapping engines, permanent exceptions); 6 sources.
- [[api-gateways-and-service-mesh]] `stub → mature`: north-south (gateway) vs. east-west (mesh) traffic distinction; gateway responsibilities table (auth, rate limiting, SSL, routing, transformation, caching, observability); gateway product landscape (Kong, AWS API Gateway, Azure APIM, Google/Apigee, Traefik, Envoy Gateway); Kubernetes Gateway API (GA 1.28) role-oriented model (GatewayClass/Gateway/HTTPRoute) as convergence standard; sidecar mesh architecture (Envoy sidecar + control plane); mesh capabilities (mTLS, traffic management, retries/circuit breaking, distributed tracing, L7 auth policy); service mesh comparison table (Istio/Linkerd/Cilium/Consul — proxy, architecture, standout); Istio Ambient mode GA 2024 (ztunnel L4 per-node + Waypoint proxy L7 per-service; ~50% memory reduction); Cilium eBPF best L4 performance; Envoy Gateway as unified north-south + east-west data plane; gateway vs. mesh decision table (9 scenarios); convergence trend (same HTTPRoute for ingress and mesh); pitfalls (mesh for mTLS only, no Gateway API, mesh without observability); 6 sources.
- [[event-sourcing-and-cqrs]] `stub → mature`: CRUD vs. event sourcing fundamental difference (current state vs. sequence of changes); append-only immutable event log as source of truth; event replay to reconstruct any past state; CQRS write/read model separation; projection mechanics (eventual consistency, independent stores, purpose-built per query); why it matters (audit logs, temporal queries, multiple read models, write throughput, regulatory compliance); when NOT to use (warning callout — most systems, small teams); event store options (EventStoreDB, Kafka, PostgreSQL append-only, SQL Server 2025/Polecat); snapshot optimization (load nearest snapshot + tail events); command handling pattern (validate → produce events → append with optimistic concurrency); CQRS without event sourcing (simpler option — read replica or denormalized views); event schema evolution strategies (upcasters, versioned types, weak schema/forward compatibility); AI agent state management application (event sourcing for agent action audit/replay); pitfalls (global application, storing derived state in events, mutating events, no snapshots, ignoring projection lag); 5 sources.
- [[data-governance-and-lineage]] `stub → mature`: four governance pillars (catalog, lineage, contracts, access governance); regulatory drivers (GDPR, CCPA, EU AI Act enforcement 2025, HIPAA, SOX); catalog product comparison table (Unity Catalog/DataHub/Apache Atlas/Microsoft Purview/Collibra/Alation); lineage granularity levels (table/column/row with use cases); OpenLineage as open standard (CNCF, Airflow/Spark/dbt/Flink emitters, catalog-agnostic); Unity Catalog automatic lineage for all SQL + Delta Live Tables (no instrumentation); DataHub post-LinkedIn (2025 independent, 3,000+ orgs, OpenLineage native, column-level lineage graph); data contracts (schema/SLAs/semantics/breaking-change policy, ODCS/Soda/YAML-in-CI enforcement); data quality dimensions (completeness/accuracy/freshness/consistency/uniqueness) + tools (Great Expectations, dbt tests, Soda, Monte Carlo/Anomalo); column masking + attribute-based sensitivity tagging; AI asset governance (Unity Catalog model/feature/eval dataset governance; EU AI Act compliance for high-risk AI); platform-native vs. platform-independent catalog trade-off; data mesh governance principle (federated ownership, centralized infrastructure); Baumann 2025 market report (42 vendors, acquisition consolidation wave); pitfalls (manual catalog, table-only lineage, contracts without enforcement, centralized ownership at scale, no classification); 6 sources.
- Updated `index.md`: 34 mature pages, 1 draft, 32 stubs remaining (230+ cited sources total).

## [2026-06-20] build | Wave 4 — 4 security cluster stubs promoted to mature

- [[zero-trust-architecture]] `stub → mature`: NIST SP 800-207 PE/PA/PEP model; CISA five-pillar model (Identity/Devices/Networks/Applications/Data); identity-first access (ZTNA replacing VPN); BeyondCorp reference implementation; microsegmentation (host-based/network-based/Kubernetes NetworkPolicy); continuous/context-aware authentication; JIT access; non-human identity (NHI) expansion (NSA 2026 NHI discovery mandate, NIST IR 8596 machine-to-machine ZTA); AI agent ZTA patterns (delegated scopes, time-bounded sessions, per-action gates); 60% large enterprise ZTA adoption by 2026; $31.6B market; cloud provider ZTA service mapping; arXiv:2511.04925 ZTA in microservices with identity federation; 6 sources.
- [[iam-and-secrets-management]] `stub → mature`: authentication vs. authorization distinction; MFA (TOTP vs. FIDO2/passkeys — phishing resistance); RBAC vs. ABAC (attributes, hybrid model, when ABAC justified); least-privilege + JIT elevation + permission boundaries; workload identity federation (IRSA/GCP WIF/Azure Managed Identity/GitHub OIDC → cloud credentials table); OAuth 2.0 RFC 8693 token exchange; NHI inventory as prerequisite (NSA 2026, NIST IR 8596); dynamic credentials (Vault lease-based database passwords); secrets vaults comparison (HashiCorp Vault/AWS Secrets Manager/SSM/Azure Key Vault/GCP Secret Manager); secret injection patterns (env vars/mounted volumes/SDK fetch); PAM (JIT elevation, session recording, break-glass); AI agent IAM (delegated scopes, time-bound, per-action confirmation, distinct audit trail); keyless signing via Fulcio + Rekor; anti-pattern: secrets in state files; 6 sources.
- [[prompt-injection]] `stub → mature`: OWASP LLM01:2025 (third consecutive year as top LLM vulnerability); direct vs. indirect injection distinction; MCP expansion of attack surface (tool descriptions, tool output, memory stores, RAG results); attack taxonomy table (instruction override/data-borne/RAG poisoning/tool output injection/prompt leaking/jailbreaking/multi-agent relay); why defenses are hard ("Attacker Moves Second" — 12 published defenses bypassed at >90% by adaptive attacks); five-layer defense-in-depth (input sanitization → structural prompt hardening → retrieval isolation → output validation + action gates → least privilege); AttriGuard causal attribution (arXiv:2603.10749); SecInfer inference-time detection (arXiv:2509.24967); tool result isolation (arXiv:2601.04795); RAG poisoning (5 docs = 90% manipulation rate, Jan 2026); 6 sources.
- [[software-supply-chain-security]] `stub → mature`: threat model (SolarWinds/Log4Shell/XZ Utils/GhostAction/Shai-Hulud timeline); regulatory drivers (US EO 14028, EU CRA, NIST SSDF); SBOM (SPDX vs. CycloneDX formats, what SBOMs do/don't guarantee); SLSA 4-level framework table (what each requires, key guarantee); SLSA GitHub Generator for Level 3 in 1-2 days; Sigstore/Cosign keyless signing (Fulcio OIDC certificates, Rekor transparency log, keyless workflow); full chain of trust (SBOM + SLSA provenance + Cosign signature); SCA tools (Grype, Trivy, Snyk, Dependabot); dependency confusion + typosquatting mitigations (namespace scoping, private registry priority, lockfiles); build system hardening (hermetic/reproducible builds, least-privilege CI, separate build/deploy credentials); GitHub Actions pin-by-SHA (GhostAction exploitation of mutable tags); Sonatype 2025: 454K malicious packages; AI-generated code risk; model supply chain link; 6 sources.
- Updated `index.md`: 30 mature pages, 1 draft, 36 stubs remaining.

## [2026-06-20] build | Wave 3 — 6 reliability + data + platform stubs promoted to mature

- [[observability-fundamentals]] `stub → mature`: three pillars (metrics/logs/traces) + OpenTelemetry architecture (API/SDK, OTLP, semantic conventions, Collector); RED method; Prometheus + managed equivalents; Fluent Bit/Loki/OpenSearch log aggregation; distributed tracing (head vs. tail sampling, W3C Trace Context propagation); SLI/SLO/error budget mechanics (43.8 min budget at 99.9% / 30 days); multi-window multi-burn-rate alerting (14× fast burn, 6× slow burn); cardinality management; OTel auto-instrumentation; profiles as fourth signal; AI-augmented observability state of the art; 6 sources.
- [[distributed-systems-reliability]] `stub → mature`: failure mode taxonomy table (timeout/crash/partial/corruption/cascade/resource exhaustion); timeout discipline + timeout budget propagation; retry with exponential backoff + jitter (thundering herd problem); circuit breaker (closed/open/half-open state machine); Resilience4j/Polly/Istio implementations; bulkhead pattern (thread pool, connection pool, service instance); graceful degradation + load shedding (admission control, priority queues, back-pressure); RTO/RPO table (active-active to backup-only cost vs. recovery); replication lag as RPO floor; chaos engineering (hypothesis → inject → observe → compare); tools (Gremlin, Steadybit, AWS FIS, Chaos Mesh); sovereign fault domains (InfoQ 2026); durable execution (Temporal); arXiv:2512.16959v1 systematic review; 6 sources.
- [[streaming-and-event-data]] `stub → mature`: event log model (durability/multi-consumer/replay/ordering) vs. queues; streaming platform comparison table (Kafka/Kinesis/Pub Sub/Event Hubs/Pulsar across retention/ordering/managed offerings); delivery semantics (at-most-once/at-least-once/exactly-once); Kafka exactly-once scope warning (Kafka-to-Kafka only via transactional producer API); Flink architecture (event-time semantics, Chandy-Lamport checkpointing, watermarks, savepoints); stream processor comparison table (Flink/Kafka Streams/Spark Structured Streaming across latency/state/ops model); windowing types (tumbling/sliding/session + event vs. processing time); backpressure; partition count planning; schema evolution + schema registry requirement; Flink SQL + streaming lakehouse trend; RisingWave/Materialize as emerging alternatives; 5 sources.
- [[data-storage-paradigms]] `stub → mature`: OLTP vs. OLAP workload split; data warehouse (columnar, Snowflake/BigQuery/Redshift); data lake (raw object storage, no ACID, small file/update problems); lakehouse (open table format on object storage = ACID + time travel + schema evolution + multi-engine); open table format comparison (Iceberg = open standard, AI/agent-ready; Delta Lake = Databricks-native; Hudi = CDC/upsert-heavy); practical multi-format reality (Hudi for CDC, Delta for analytics, Iceberg for cross-engine); data mesh (domain ownership, data as product, self-serve platform, federated governance); storage/compute separation; catalog layer (Glue, Unity Catalog, Polaris); DuckDB as emerging OLAP runtime; Apache XTable interoperability; 5 sources.
- [[infrastructure-as-code]] `stub → mature`: declarative vs. imperative IaC models; tool landscape table (OpenTofu/Terraform/Pulumi/CDK/Bicep/Crossplane); OpenTofu as open-source Terraform fork (identical HCL, CNCF governance); Pulumi language advantages; AWS CDK CloudFormation synthesis; remote state as mandatory (S3+DynamoDB, GCS, Terraform Cloud); state file security (plaintext secrets, restrict backend access); drift detection (scheduled plan runs every 4-6h, alert on non-empty diff); module design (single responsibility, pinned versions, environment promotion via variables); secrets hygiene (never output secrets, write to secrets manager at apply time); policy gates (OPA/Conftest, Checkov/tfsec, Sentinel); GitOps for IaC (Atlantis, Terraform Cloud agent); AI-assisted IaC generation state; 5 sources.
- [[cicd-pipeline-architecture]] `stub → mature`: CI/CD/continuous deployment distinction; CI pipeline stages (build → unit/integration tests → SAST/SCA/image scan/IaC scan → artifact publish → policy gates); GitHub Actions/GitLab CI/Tekton platform landscape; CD environment promotion (immutable artifact, staging mirrors prod topology); GitOps pull-based model (Argo CD/Flux); progressive delivery: canary (Argo Rollouts, Flagger), blue-green (load balancer cutover), feature flags (LaunchDarkly, OpenFeature), ring deployment; DORA metrics table (deployment frequency/lead time/change failure rate/MTTR with elite thresholds: multiple/day, <1h, <5%, <1h); SBOM (SPDX/CycloneDX), image signing (Cosign/Sigstore), SLSA provenance; AI-augmented CI/CD (arXiv:2508.11867); supply chain security integration; 6 sources.
- Updated `index.md`: 26 mature pages, 1 draft ([[recursive-language-models]]), 40 stubs remaining.

## [2026-06-20] build | Wave 2 — 6 cloud + integration stubs promoted to mature

- [[multi-cloud-architecture]] `stub → mature`: 82% enterprise adoption stat; provider service equivalency table (S3/Blob/GCS, EKS/AKS/GKE, Spanner no-equivalent warning); lock-in tier taxonomy (high = DynamoDB/BigQuery/Spanner, lower = Kubernetes/Postgres-compatible); portability strategies (Kubernetes manifests, Anthos/Arc, Terraform, MinIO); data gravity + egress cost implications; active-active vs. active-passive vs. workload-partitioned topology table; multi-cloud GPU orchestration (Airbnb 12K GPU / 47% cost reduction); identity federation; 5 sources.
- [[event-driven-architecture]] `stub → mature`: producer/broker/consumer model; broker comparison table (Kafka, SQS Standard/FIFO, EventBridge, Service Bus, Pub/Sub, RabbitMQ) across throughput/ordering/delivery; exactly-once myth + at-least-once + idempotent-consumer = effectively-exactly-once; Kafka exactly-once scope limited to Kafka-to-Kafka; idempotency patterns (dedup table, conditional writes, natural idempotency); transactional outbox pattern; choreography vs. orchestration trade-off; AsyncAPI 3.0 as contract standard (73% European carrier integrations migrating); arXiv:2512.16146 Kafka benchmark taxonomy; 6 sources.
- [[serverless-architecture]] `stub → mature`: FaaS + BaaS execution model; cold start comparison table by platform (Lambda/GCF/Cloudflare Workers); SnapStart and V8 isolate mitigations; execution limits table (15 min Lambda, 60 min GCF 2nd gen); GB-second billing model; statelessness patterns; scale-to-zero cost model vs. containers (crossover analysis); provisioned concurrency decision; AI integration pattern (Lambda as orchestration glue around token-generating API calls); 70%+ AWS users run Lambda in prod (Datadog 2025); Cloud Run / Container Apps as middle ground; 4 sources.
- [[kubernetes-at-design-level]] `stub → mature`: core abstractions table (Pod/Deployment/StatefulSet/DaemonSet/Job/Service/Ingress/ConfigMap/PVC/Namespace); control plane components; Operator pattern (CRD + controller + reconcile loop); multi-tenancy spectrum (namespace soft → vCluster → separate clusters); managed provider comparison (GKE/EKS/AKS strengths, IRSA, Workload Identity, GKE Sandbox); Cilium + eBPF replacing kube-proxy; GPU scheduling (NVIDIA device plugin, MIG, time-slicing, Volcano/Run:AI gang scheduling); 82% container users on K8s (CNCF 2025); when NOT to use K8s decision; 6 sources.
- [[api-styles-and-protocols]] `stub → mature`: REST (83% public APIs, stateless/cacheable, over/under-fetch problem); GraphQL (61%+ enterprises, schema + Federation, N+1 problem, introspection security); gRPC (60–80% bandwidth reduction, Protobuf/HTTP2, streaming, Netflix/Uber/Google standard); AsyncAPI 3.0 contract standard; webhook fragility (73% European integrations migrating away); WebSockets for real-time; tRPC for full-stack TypeScript; selection decision matrix (7 scenarios); hybrid architecture by layer (public REST / BFF GraphQL / internal gRPC / async AsyncAPI); MCP as agent-to-tool protocol; schema evolution rules per style; 6 sources.
- [[cloud-native-patterns]] `stub → mature`: synthesis page tying the cluster together; Twelve-Factor App principles (config-in-env, stateless processes, logs-as-streams violations); event-driven decomposition; compute pattern table by workload (FaaS / managed containers / CronJob / GPU); resilience patterns (circuit breaker, exponential backoff + jitter, graceful degradation, bulkhead, health checks, timeout discipline); managed services table (replacing self-operated DB/cache/queue/secrets/identity/search); microservices vs. modular monolith judgment; stateless vs. stateful design principle; eBPF + platform engineering as 2026 frontiers; 5 sources.
- Updated `index.md`: 20 mature pages, 47 stubs remaining.

## [2026-06-20] build | Wave 1 — 4 P0 data/finops stubs promoted to mature

- [[ai-gpu-economics]] `stub → mature`: token pricing table (Claude Opus 4.8 $5/$25, Sonnet 4.6 $3/$15, Haiku 4.5 $1/$5; GPT-5.5 $5/$30; Gemini 2.5 Pro $1.25/M); GPU infrastructure (H100 $2.85–3.50/hr, H200 $2.15–6.00/hr); quantization economics (INT8 −50% memory, 95–99% accuracy); continuous batching 2–3× throughput; PagedAttention KV cache; prompt caching 90% cost reduction; self-hosted breakeven analysis; batch API 50% savings; 7 sources.
- [[vector-and-embedding-stores]] `stub → mature`: ANN algorithms (HNSW dominant, IVF, ScaNN); embedding models comparison (OpenAI, Cohere, BGE, Voyage); Matryoshka embeddings; similarity metrics; hybrid search (dense + BM25/SPLADE via RRF) — Milvus 2.5+ native BM25, Qdrant v1.9+ named vectors; metadata filtering (pre/post/filtered HNSW); ColBERT/multi-vector late interaction; cross-encoder reranking; managed vs self-hosted decision (50–100M vector migration threshold); database landscape table; Turbopuffer serverless; arXiv:2601.11557 binarization index research; 5 sources.
- [[feature-stores]] `stub → mature`: offline/online store split; point-in-time correctness (future leakage prevention); feature freshness model (batch / streaming / on-demand); feature registry; 5 architectural differentiators table; 2026 platform comparison (Feast, Tecton, Hopsworks, Vertex, SageMaker, Databricks); streaming vs. batch trade-off; GenAI workload extension (vector features); MLflow 3 integration; Databricks data quality monitoring; 6 sources.
- [[ai-data-fabric]] `stub → mature`: lakehouse substrate (Delta Lake / Iceberg, ACID, time travel, schema evolution); feature layer + embedding layer integration; data versioning (DVC 60% adoption, LakeFS, MLflow Dataset tracking); freshness SLA monitoring; governance and lineage (Unity Catalog, OpenLineage); agentic data pipelines (2026 trend); unified platform vs best-of-breed trade-off; embedding version migration pattern; convergence of feature stores and vector stores; 72% of enterprises cite data infra as #1 AI scaling blocker (DataForest 2026); Azure GenAIops-for-MLops pattern; 6 sources.
- Updated `index.md`: 14 mature pages, 53 stubs remaining.

## [2026-06-20] build | Wave 0 — 4 draft/seed pages promoted to mature

- [[multi-agent-orchestration]] `draft → mature`: expanded "State of the art" with the MAST failure-taxonomy paper (41–87% multi-agent failure rates, specification/coordination/verification gaps), added 2026 framework landscape table (LangGraph GA v1.0.10, CrewAI v1.10.1, Claude Agent SDK v0.1.48, OpenAI Agents SDK, Strands, AutoGen); added 4 new sources.
- [[context-engineering]] `seed → mature`: full rewrite from seed to a research-backed 8-source mature page. Added: context budget allocation (system/retrieved/history/generation zones), information ordering (U-shaped attention / lost-in-the-middle, Liu et al. arXiv:2307.03172 + calibration study arXiv:2406.16008), compression patterns (sliding window / hierarchical summarization / selective preservation), prompt caching design (cache_control breakpoints, 90% cost reduction), relevance selection (top-k, MMR, reranking, late chunking), ContextBudget runtime control (arXiv:2604.01664), AgentSwing adaptive routing (arXiv:2603.27490), LOCA-bench context-rot benchmark (arXiv:2602.07962). Anchored in Anthropic's Sept 2025 "Effective context engineering" post.
- [[agent-memory-architectures]] `draft → mature`: content already at mature quality (3-tier memory, POLE+O ontology, 3-stage extraction, entity normalization, dream pipeline, 8 sources); status updated.
- [[graphrag]] `draft → mature`: added GraphRAG vs. plain RAG decision table (query type / infrastructure cost / latency / multi-hop / best-fit dimensions); all cross-links to [[retrieval-augmented-generation]] and [[agent-memory-architectures]] verified; status updated.
- Updated `index.md`: 10 mature pages (70+ cited sources), 0 draft, 0 seed.

## [2026-06-20] build | Gap analysis — 4 missing roadmap nodes added as stubs

- Audited all 63 concept pages against `skill-set/2026/technology-skills.md`; found 4 roadmap nodes with no wiki page.
- Created [[encryption-and-key-management]] `stub` (§3.1.3, security) — envelope encryption, KMS, HSM, mTLS, CMK, secrets management.
- Created [[network-segmentation]] `stub` (§3.1.4, security) — VPC segmentation, security groups, Kubernetes Network Policy, eBPF enforcement, mTLS east-west.
- Created [[dynamic-team-staffing]] `stub` (§9.2, emerging) — agentic throughput layer, human judgment gates, Team Topologies framing.
- Created [[ai-architecture-certifications]] `stub` (§9.3, emerging) — vendor AI architect tracks (AWS/Azure/GCP), nascent agentic-AI programs, TOGAF extensions.
- Updated `index.md`: all 67 concept pages now registered; roadmap coverage is complete (every §1–§9 + meta node has a page).
- Priority upgrade queue identified (plan file): Wave 0 = promote 4 in-flight drafts; Wave 1 = ai-data-fabric + ai-gpu-economics cluster; Wave 2 = multi-cloud + cloud-native cluster; Wave 3 = reliability + streaming + data + CI/CD.

## [2026-06-19] build | House-style change — drop priority/roadmap, de-frame prose

- Removed `priority` and `roadmap_ref` frontmatter from all wiki pages, MOCs, templates, index, and the source note; trimmed every concept context line to **Domain only**.
- Renamed sections `## Why it matters (2026, senior architect lens)` → `## Why it matters` and `## State of the art (2026)` → `## State of the art`.
- De-framed prose across the wiki: removed persona/role framing ("senior architect", "veteran", "15+ year") and editorial-year framing ("the 2026 consensus"), keeping factual year tokens (standard names, GA/release dates, citation titles, URLs, ISO dates).
- `index.md` dropped Pri/Roadmap columns; MOCs dropped priority emojis + `§` refs; [[dashboard]] re-based on status/domain (Bases) instead of priority. Concepts stay flat.
- Encoded the rules as **House style** in `CLAUDE.md` §8 and added a house-style check to `scripts/lint.ps1` (priority/roadmap, year-in-heading, role terms). Lint: PASS (0 broken, 0 house-style violations).
